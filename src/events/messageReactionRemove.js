const { Events } = require('discord.js');
const { starboardMessages } = require('./messageReactionAdd');

module.exports = {
  name: Events.MessageReactionRemove,
  async execute(reaction) {
    /* ***** Starboard Remove***** */
  },
};
